/**
 * @module Analyze
 */

/**
 * Ignore
 */
import Ship from './Ship';

export function analyzeDefences(
    ship: Ship,
    opponent: Ship,
    engagementRange: number,
): any {}

export function analyzeOffences(
    ship: Ship,
    opponent: Ship,
    engagementRange: number,
): any {}

export function compareOn(
    ship: Ship,
    otherShip: Ship,
    properties: string[],
): any {}

export function moduleCompareOn(
    module: Ship,
    otherModule: Ship,
    properties: string[],
): any {}
